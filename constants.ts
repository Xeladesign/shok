import { Product } from './types';

export const CATEGORIES: string[] = ['הכל', 'ערכות UI', 'תבניות', 'אייקונים', 'סושיאל', 'תלת מימד', 'אלמנטור'];

// Mock products removed to ensure only real user data is shown.
export const MOCK_PRODUCTS: Product[] = [];
